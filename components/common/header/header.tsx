import Head from "next/head";

const Header = () => {
  return (
    <Head>
      <title>Vulnerable React/Next App</title>
      <meta name="description" content="Vulnerable react/next app" />
      <link rel="icon" href="/public/favicon.ico" />
    </Head>
  );
};

export default Header;
